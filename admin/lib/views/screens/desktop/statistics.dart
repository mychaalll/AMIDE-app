import 'package:flutter/material.dart';

class DescktopStatScreen extends StatelessWidget {
  const DescktopStatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("Stats"),
    );
  }
}
